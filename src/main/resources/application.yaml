spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lono_dev_test1?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: admin222
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true

sql:
  init:
    mode: always

server:
  port: 8080

jwt:
  secret: minha-chave-jwt-super-segura-com-mais-de-32-bytes-123456
  expiration: 86400000